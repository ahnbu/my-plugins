# Plan: 문서/_handoff 경로 통일

> 저장일: 2026-03-06 | 원본: C:\Users\ahnbu\.claude\plans\serialized-dazzling-galaxy.md

## 개요

프로젝트 내 문서와 handoff 파일이 3곳에 산재되어 있고, 글로벌 CLAUDE.md ↔ 프로젝트 CLAUDE.md ↔ wrap 스킬 간 경로 규칙이 불일치. 사용자가 **루트 `_docs/`, `_handoff/`로 통일**을 요청.

## Context

### 현재 상태 진단

**handoff 분포 (36개)**
| 위치 | 파일 수 | 비고 |
|------|---------|------|
| `_handoff/` (루트) | 31개 | wrap 스킬이 실제 저장하는 위치 |
| `_docs/my-session-wrap/_handoff/` | 5개 | 프로젝트 CLAUDE.md 규칙대로 된 유일한 곳 |

**문서(_docs 외) 산재 (3개)**
| 위치 | 파일 |
|------|------|
| `my-session-wrap/_docs/` | `20260305_wrap-스크립트경로-이식성수정_plan.md` |
| `my-session-dashboard/` (루트) | `20260305_세션대시보드_경로복사_계획.md`, `20260306_세션대시보드_로컬서버_계획.md` |

**규칙 충돌**
| 소스 | handoff 경로 규칙 |
|------|-------------------|
| 글로벌 CLAUDE.md (L50) | `_handoff/handoff_YYYYMMDD_NN_*.md` (루트) |
| 프로젝트 CLAUDE.md (L93) | `_docs/<plugin-name>/_handoff/handoff_YYYYMMDD_NN_*.md` |
| wrap 스킬 SKILL.md (L108) | `<ProjectRoot>/_handoff/handoff_YYYYMMDD_NN_*.md` (루트) |

→ 글로벌 규칙과 wrap 스킬은 이미 **루트 `_handoff/`**를 가리킴. 프로젝트 CLAUDE.md만 다른 경로를 정의 중.

## 변경 계획

### Step 1: 프로젝트 CLAUDE.md — Handoff 섹션 삭제
- **L90-98** `### Handoff 문서` 섹션 전체 삭제
- 글로벌 CLAUDE.md(L50)에 동일 규칙이 이미 있으므로 중복 정의 자체를 제거
- 프로젝트 특화 내용이 없으므로 삭제가 수정보다 깔끔

### Step 2: 산재 파일 이동

#### 2-1. handoff 5개 → 루트 `_handoff/`로 이동
```
_docs/my-session-wrap/_handoff/handoff_20260225_09_*.md → _handoff/
_docs/my-session-wrap/_handoff/handoff_20260225_10_*.md → _handoff/
_docs/my-session-wrap/_handoff/handoff_20260225_11_*.md → _handoff/
_docs/my-session-wrap/_handoff/handoff_20260225_12_*.md → _handoff/
_docs/my-session-wrap/_handoff/handoff_20260302_01_*.md → _handoff/
```
이동 후 `_docs/my-session-wrap/_handoff/` 빈 폴더 삭제.

#### 2-2. 플러그인 내부 문서 → `_docs/`로 이동
```
my-session-wrap/_docs/20260305_wrap-스크립트경로-이식성수정_plan.md
  → _docs/my-session-wrap/20260305_wrap-스크립트경로-이식성수정_plan.md

my-session-dashboard/20260305_세션대시보드_경로복사_계획.md
  → _docs/my-session-dashboard/20260305_세션대시보드_경로복사_계획.md

my-session-dashboard/20260306_세션대시보드_로컬서버_계획.md
  → _docs/my-session-dashboard/20260306_세션대시보드_로컬서버_계획.md
```
이동 후 `my-session-wrap/_docs/` 빈 폴더 삭제.

### Step 3: 기타 정리
- `_docs/my-session-wrap/task_plan.md` — planning-with-files 스킬 작업 파일이므로 **그대로 유지**
- `_docs/my-session-wrap/.claude/.current-session-id` — 임시파일, 삭제 대상

### Step 4: 커밋 (2개 분리)
1. `docs(project): CLAUDE.md handoff 경로 규칙 통일 — 루트 _handoff/로 일원화`
2. `chore(project): 산재 문서/_handoff 파일을 루트 _docs/_handoff로 이동`

## 변경하지 않는 것
- 글로벌 CLAUDE.md — 이미 올바른 규칙
- wrap 스킬 SKILL.md — 이미 올바른 경로
- `_docs/` 내부 서브폴더 구조 (`_docs/<plugin-name>/`) — 유지

## 보충: 대화 중 검토 내용

### 의사결정 근거

**Step 1 — 수정 대신 삭제를 선택한 이유**

| 옵션 | 설명 | 판단 |
|------|------|------|
| 경로를 `_handoff/`로 수정 | 프로젝트 CLAUDE.md에 handoff 경로 규칙 유지 | ⚠️ 글로벌과 중복 |
| ✅ 섹션 전체 삭제 | 프로젝트 특화 내용 없으므로 글로벌에 위임 | ✅ 깔끔, 중복 제거 |

프로젝트 CLAUDE.md 원칙: "해당 프로젝트 특화 내용만 작성 (글로벌 CLAUDE.md 규칙과 중복 금지)". Handoff 경로는 글로벌 규칙으로 충분하므로 삭제가 원칙에 부합.

## 검증계획과 실행결과

| 검증 항목 | 검증 방법 | 결과 | 비고 |
|-----------|-----------|------|------|
| CLAUDE.md Handoff 섹션 삭제 | L90-98 미존재 확인 | ✅ 완료 | grep 결과 0건 |
| handoff 5개 이동 | `_handoff/` 파일 수 36개 확인 | ✅ 완료 | 37개 (기존 31 + 5 이동 + 미추적 1) |
| 플러그인 내부 문서 3개 이동 | `_docs/` 내 파일 확인 | ✅ 완료 | `_docs/my-session-wrap/`, `_docs/my-session-dashboard/` 확인 |
| 빈 폴더 삭제 | `my-session-wrap/_docs/`, `_docs/my-session-wrap/_handoff/` 미존재 | ✅ 완료 | No such file or directory 확인 |
| 임시파일 삭제 | `.current-session-id` 미존재 확인 | ✅ 완료 | `.claude/` 폴더 포함 삭제 |
| 커밋 2개 생성 | git log 확인 | ✅ 완료 | 7fe039c, 0594374 |
